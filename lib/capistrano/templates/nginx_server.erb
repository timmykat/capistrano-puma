# For more information on configuration, see:
#   * Official English Documentation: http://nginx.org/en/docs/
#   * Official Russian Documentation: http://nginx.org/ru/docs/

user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log;
pid /var/run/nginx.pid;

# Load dynamic modules. See /usr/share/doc/nginx/README.dynamic.
include /usr/share/nginx/modules/*.conf;

events {
    worker_connections 1024;
}


http {
  include       mime.types;
  default_type  application/octet-stream;

  sendfile        on;

  keepalive_timeout  65;

  gzip  on;

  # Set file upload limits
  client_max_body_size 2M;
  <% path = Gem.loaded_specs["capistrano3-puma"].gem_dir + "/lib/capistrano/templates/server.erb" %>
  <% puts path %>
  <% server_content = path.result(binding) %>%
  <% server_config = ERB.new(server_content).result(binding) %>%
  <%= ERB.new(sub_template_content).result(binding) %>
}